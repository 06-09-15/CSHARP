//For loop
using System;

class Program {
  public static void Main () {
  
  int num, sum = 0;
  for (num = 0; num <= 49;)
  {
  num = num + 1;
  sum = sum + num;
  }
  Console.WriteLine("Sum = {0}", sum);
  Console.ReadLine();
  }
}

//While loop
using System;

class Program {
  public static void Main () {
  
  int i = 0;
  int sum = 0;
  while(i <= 50)
  {
    sum = i + sum;
    i++;
  }
  Console.WriteLine("Sum = "+ sum);
  Console.ReadLine();
  }
}
    
